# FlorrVLM-Agent 开发路线图（ROADMAP）

> 从 Florr.io 专用智能体出发，逐版本演进，最终成为通用游戏智能体。
> 核心能力贯穿始终：VLM 视觉感知 / 实体运动预判 / MCP 本地 MD 知识库 / 视频自学 / 死亡复盘 / 临时文件自动清理 / 低资源占用。

## 版本总览

| 版本 | 定位 | 状态 |
|------|------|------|
| v0.1 | Florr.io 初版 | ✅ 已完成 |
| v0.2 | 稳定性补丁 | ✅ 已完成 |
| v0.3 | 基础战术扩充 | ✅ 已完成 |
| v0.4 | 基础学习闭环升级 | ✅ 已完成 |
| v0.5 | 完整 MCP 生态（1.0 前最后内测） | ✅ 已完成 |
| v0.6 | Agent 形态正式化（能对话、能自我说明） | ✅ 已完成 |
| v0.7 | MCP Client 能力（主动接外部 MCP） | ✅ 已完成 |
| v0.8 | Skill 能力机制（能接/加载技能包） | ✅ 已完成 |
| v0.9 | 全通用化（游戏档案化 + 核心解耦） | ✅ 已完成 |
| **v1.0** | **稳定底座版：真正可用的通用 Agent，能接外部 MCP + Skill，像普通 Agent 一样工作** | ✅ 功能就绪(待实机验收) |
| v1.1 | 可视化监控大盘（正式监控页：指标卡片 / BOSS 预判轨迹 / 日志面板 / 资源占用） | ✅ 已完成 |
| v1.2 | 自动汇报 & 多渠道通知（每局结束自动出报告，可推送本地/Webhook） | ✅ 已完成 |
| v1.3 | 一键登记新游戏档案（交互向导生成 game_profiles/<name>.yaml，零改核心） | ✅ 已完成 |
| v1.4 | 断点续玩 & 会话记忆（重启自动恢复进度并汇报续玩） | ✅ 已完成 |
| v1.5 | 多局战绩历史 & 汇总统计（每局自动入档，stats 命令看汇总与最近战绩） | ✅ 已完成 |
| v1.6 | 大盘集成会话记忆 & 战绩（监控页加场次/回合卡与多局战绩栏） | ✅ 已完成 |
| v1.7 | 局中定时进度汇报（report_every 轮间隔，覆盖式单文件+Webhook，不阻塞主循环） | ✅ 已完成 |
| v1.8 | 游戏档案自检器（validate 命令 / add_game 生成后即检，投bo前防格式错） | ✅ 已完成 |
| v2.0 | 正式深度开发 Florr.io 专属高级功能（拟人化高水平玩家） | 📌 概述 |
| v3.0 | 转折点：转为通用 2D 游戏智能体 | 📌 概述 |
| v4.0 | 完美适配全部 2D 游戏，自学习适配 | 📌 概述 |
| v5.0 | 开始支持 3D 游戏 | 📌 概述 |

---

# 详细开发阶段（v0.3 → v1.0）

## v0.3 —— 基础战术扩充

> 目标：让 Agent 从"会打"变成"会选打法"，面对不同情况自动换战术。

### 1. 多套装自动切换系统
- 内置 4 套基础战术配置：**战斗套 / 跑路套 / 追击套 / 组队辅助套**
- 根据战斗评估结果自动切换（战斗评估已产出 `recommended_set`，本版真正落地切换动作）
- 套装切换动作加入 `game_action` 工具，通过按键/坐标点击完成

### 2. BOSS 习性记忆增强
- 不只是记录坐标轨迹，升级为记录**行为模式**：移动模式、攻击前摇、仇恨切换、追击距离
- 每 12 秒批量写入 `knowledge_md/boss_behavior_log.md`
- 同一种 BOSS 多次遭遇时自动归纳共性（如"Super 黄蜂喜欢绕圈"）

### 3. 简单组队协同
- 从画面实体中识别队友（同阵营标识）
- 基础分工规则：队友输出套 → 我方辅助拉扯；队友抗伤套 → 我方全力输出
- 保持安全距离跟随队友，不扎堆

### 4. 拟人操作增强
- 鼠标移动抖动已实现，本版加入**移动路径微扰**：目标点附近小范围随机二次寻路，消除"笔直冲向目标"的机器感
- 加入随机停顿：战斗中偶发 100~300ms 停顿，模拟人类反应

### 5. 安全区检测
- 检测屏幕边缘与障碍物，防止无脑贴墙卡死
- 走位目标自动限制在安全区域内（不触发鼠标角落暂停的范围内）

### 6. 战术记忆初步
- 每次换套动作记入知识库 `player_tactics.md`，形成"什么情况用什么套"的长期经验

---

## v0.4 —— 基础学习闭环升级

> 目标：Agent 能自己"找教程 → 学打法 → 上实战"，形成完整自学循环。

### 1. 教程视频自动获取
- 从社交平台（B站 / 抖音 / YouTube 等）自动检索 florr.io 教程视频
- 下载 → 抽帧 → VLM 提取战术 → 写入知识库 → **学完立刻删除视频源文件**
- 延续"小硬盘友好"原则：任何临时文件用完即删

### 2. 知识库去重与整理
- 相似战术笔记自动合并（关键词 + 文本相似度判断）
- 自动归档：`player_tactics.md` / `boss_behavior_log.md` / `review_*.md` / `video_tactic_*.md` 分类存放

### 3. 复盘系统升级
- 死亡复盘时**自动检索历史相似对局**（同怪物 / 同套装 / 同地图区域）
- 对比出改进点："上次同 Boss 是跑掉的，这次是硬拼死的"
- 复盘结论写回知识库，供下次决策引用

### 4. 玩家移动预判
- 预判模块从"只预判怪物"扩展到**同时预判其他玩家**的移动意图
- 玩家实体单独分类（player_enemy / player_ally），威胁评估差异化

### 5. 学习效果验证
- 记录每次对局的"知识库命中次数"（决策时检索到了几条有用战术）
- 命中率低的领域自动标记，优先补充该方向教程

---

## v0.5 —— 完整 MCP 生态（1.0 前最后内测）

> 目标：模块化、可配置、可观测，为 1.0 稳定版收尾。

### 1. 完整 MCP 工具集
| 工具 | 功能 |
|------|------|
| `kb_list / kb_search / kb_write / kb_append` | 知识库读写 |
| `perceive_game` | 游戏状态感知 |
| `predict_all_entities` | 全实体预判 |
| `reset_predictor` | 重置预判 |
| `game_action` | 键鼠动作（含套装切换） |
| `handle_afk` | AFK 验证处理 |
| `switch_tactic` | 切换战术套装 |
| `query_boss_history` | 查询 BOSS 历史习性 |
| `clean_cache` | 清理临时文件 |

### 2. 外部 MCP 客户端接入
- 你的本地 Agent 可以通过 MCP 直接调用本项目的全部工具
- 提供标准 MCP 配置文件示例

### 3. 参数配置抽离
- 所有阈值参数（预判秒数 / 置信度 / 威胁分 / 防抖间隔 / 日志大小）从代码抽到 `config.yaml`
- 改参数不用改源码，热加载生效

### 4. 简易本地面板
- 浏览器打开本地页面，实时查看：当前状态 / BOSS 预判轨迹 / 自身实力评分 / 知识库检索记录 / 决策日志

### 5. 日志治理
- 日志独立目录 `run_logs/`，按天滚动
- 超过保留期自动压缩，超期自动删除，硬盘占用有上限

### 6. 一键启动脚本
- `start_all.sh`：一条命令启动感知服务 + MCP 服务 + Agent 主循环
- `stop_all.sh`：优雅停止并清理临时文件

---

## v0.6 —— Agent 形态正式化（能对话、能自我说明）

> 目标：不再是"后台跑脚本"，而是一个**能被你对话、能汇报、能被编排**的 Agent，和普通 Agent 平级。

### 1. 交互式入口（对话指挥）
- 新增 `agent_cli.py`：命令行可直接对话——输入 `帮我去查 Florr 资料`、`你现在打到哪了`、`把最近战况汇报一下`
- Agent 出现"问-答"能力：你问什么，它答什么，不再只是一直循环

### 2. 能力清单（自我说明）
- Agent 能输出"我会干什么"：列出已注册的 MCP 工具、Skill、知识库、当前在哪张游戏档案
- 增加 `describe_capabilities` 工具，LLM 或你都能随时问"你能做什么"

### 3. 会话状态管理
- 持久化会话：跨进程记住"上次玩到哪、上次学了什么、上次配置是什么"
- 状态文件 `agent_state.json`，启动自动恢复，不丢上下文

### 4. 统一的 Agent 生命周期
- 把所有散落脚本收敛成标准阶段：`detect(这是什么游戏) → research(去查) → ensure(确认我能玩) → play(执行) → report(汇报)`
- 每个阶段可被对话单独触发，也可整条链路自动跑

---

## v0.7 —— MCP Client 能力（主动接外部 MCP）

> 目标：Agent **不只是被调用的 Server，还能主动去连别人的 MCP**，把外部能力变成自己的。

### 1. 外部 MCP 连接配置
- 新增 `mcp_connectors.yaml`：列出需要主动连接的外部 MCP（如搜索、工具、别的 Agent）
- 每条含：名称 / 启动命令 / 参数，跟 `mcp_client_config.example.json` 同格式

### 2. 启动即自动连接
- Agent 启动时读取 `mcp_connectors.yaml`，逐个 `stdio_client` 连接
- 连接成功的工具列表合并进 LLM 可调用集合；连不上则跳过并提示，不阻塞启动

### 3. 外部工具纳入决策
- LLM 的 system prompt 动态注入外部工具摘要，让 Agent 能"用外部 MCP 帮忙"

### 4. 双向贯通
- 对外：`mcp_server.py` 继续对外暴露本机工具
- 对内：`mcp_client` 主动接外部服务 → **完成"既能被调、又能去调"的完整 Agent 闭环**

---

## v0.8 —— Skill 能力机制（能接/加载技能包）

> 目标：引入"技能包(Skill)"概念，像普通 Agent 一样**按需装配能力**，而不是把逻辑写死。

### 1. Skill 目录与格式
- 新增 `skills/` 目录，每个技能一个子目录，含 `SKILL.md`（描述 + 加载说明 + 可调用函数声明）
- 内置首批技能包：`search(去网上查游戏资料)` / `learn(看教程学打法)` / `review(复盘)` / `report(汇报)`

### 2. 动态发现与加载
- Agent 启动扫描 `skills/`，读 `SKILL.md` 注册能力
- 运行期可 `load_skill(name)` / `unload_skill(name)`，加新技能不用改核心代码

### 3. 技能编排
- LLM 决策时能看到技能列表，按需调用组合（例如：`learn` 后 `play` 再 `report`）

### 4. 外部 Skill 接入
- 支持指向外部 Skill 仓库/路径的加载（和 v0.7 的外部 MCP 呼应），让 "通用 Agent 生态" 真正互通

---

## v0.9 —— 全通用化（游戏档案化 + 核心解耦）

> 目标：把 Florr 相关的"血肉"全部抽离成**一份游戏档案**，核心跑通用骨架，不再写死任何游戏。

### 1. `game_profiles/` 游戏档案目录
- 每款游戏一份 `*.yaml`：实体分类 / 威胁分 / 稀有度 / 套装映射 / 战术模板 / 感知端口
- 第一份档案就是 `florr.yaml`（把现在 config.yaml 里的 Florr 专属部分整体搬进去）

### 2. 核心解耦
- `agent_main.py` / `predictor.py` / `combat_judge.py` 只认"当前激活档案"的语义，不认具体游戏名
- 切换游戏 = 切换一份档案 + 启动对应感知服务，核心零改动

### 3. 档案热加载
- 检测到档案改动即热重载，和现有 config 热加载同一机制

### 4. 多档案并存
- `game_profiles/` 可同时放多款游戏档案，通过 `AGENT_GAME` 环境变量或对话指定用哪份
- 为 v2.0（Florr 深度化）和 v3.0（通用 2D）铺好结构

---

## v1.0 —— 稳定底座版（里程碑）

> **核心目标：一个完整可用、能稳定跑、满足日常 Florr.io 游戏需求的通用 Agent。**
> v0.6~v0.9 打好的 Agent 骨架（对话 / 接外部 MCP / Skill / 游戏档案）在此版做实、做稳。
> 它不再只是"后台脚本"，而是**能和普通 Agent 平级工作**：你对话指挥、它能接外部 MCP、能装技能包、有游戏档案可切换。
> 此版本不做花哨功能，只保证"开箱能用、挂了会自己恢复、越打越聪明"。

> ✅ 已落地模块（v1.0 第一批）：
> - `boot_check.py` —— 启动自检 + 修复指引（`start_all.sh` 已接入，缺依赖直接拦住）
> - `watchdog.sh` + `florr-agent.service.example` —— 崩溃自动重启 + 内存上限 + systemd 7×24 守护
> - `kb_maintainer.py` —— 知识库体积上限归档 + 重复笔记合并（agent_main 启动时自动跑）
> - `Dockerfile` —— 一键部署（Xvfb 虚拟屏 + 看门狗）
> - `cli_ui.py` + `agent_cli.py` 重写 —— 彩色面板界面；开玩前 `brief` 问答了解游戏，play 前自动引导
> - `auto_tuner.py` + `tuned_overrides.yaml` —— 基础自动调参：按命中率/死亡数自动微调战斗阈值与置信线，热加载生效

### 1. 完整基础自主闭环
```
自动找教程视频 → 学习存入知识库 → 实战对局 → 死亡复盘 → 知识库自我进化 → 再实战
```
循环不断，Agent 越打越强。

### 2. 多实体预判
- 同时预判多个 BOSS + 多名玩家 + 精英怪的 1.2 秒位置，带置信度
- 预判结果按威胁排序输出前 8 个，节省 Token

### 3. 自适应战术
- 根据当前花瓣 / 地图 / 敌人组合动态选打法，不限于固定 4 套
- 决策时自动检索知识库最相关的战术笔记

### 4. 基础组队系统
- 识别队友，自动分工：拉扯 / 输出 / 掩护
- 组队对局自动生成复盘（v0.2 已有，本版补全）

### 5. 基础自动调参
- 多次对局后统计：预判命中率 / 逃跑成功率 / 同 Boss 死亡次数
- 自动微调阈值（如威胁比分、置信度线），不用手动改

### 6. 验证码处理
- AFK 人机验证弹窗自动检测 + 自动点击处理
- 处理失败时降级为"暂停等待"，不误操作

### 7. 一键部署
- 完整 README + 一键部署脚本 + Docker 打包（云电脑一条命令拉起）

### 8. 轻量知识库
- 自动压缩陈旧笔记（合并同类项 / 截断冗余）
- 知识库体积有上限，超限自动归档压缩，保护小硬盘

### 9. 稳定性保障
- 崩溃自动重启（systemd / supervisor 示例）
- 启动自检：环境依赖缺失时给出修复指引，不静默失败
- 7×24 长时间运行的内存 / CPU 占用有上限控制

### v1.0 验收标准
- [x] 一键部署：`boot_check` 自检 + `Dockerfile` + `start_all.sh` 一条命令拉起
- [ ] Agent 连续运行 24 小时不崩溃、不卡死（需实机 7×24 跑验证）
- [ ] 能独立完成一整局：找怪 → 评估 → 打/跑/追 → 死亡复盘 → 知识库有新增（需实机验证）
- [x] 硬盘占用稳定：临时文件 0 残留，`kb_maintainer.py` 保证知识库体积受控
- [x] 预判置信度可用：低置信度不瞎跑（`confidence_threshold` 阈值锁 + 自动调参）
- [x] 像普通 Agent：banner/面板对话、`brief` 问答、接 MCP、加载 Skill、`game_profiles` 切游戏零改核心

---

## v1.1 —— 可视化监控大盘（小版本）

> 目标：把"简陋的本地面板"升级成正式**监控大盘**，让不看代码也能一眼看懂 Agent 在干什么。
> 落地：`admin_panel.py` 大改 + 前端指标卡片。

- 指标卡片：当前游戏 / 会话状态 / 本局回合数 / 死亡次数 / 知识库规模 / 上次调参结论
- BOSS 预判轨迹：最近一次预判出的威胁实体按威胁值排序、实时刷新并可视化
- 日志面板：最近决策 / 知识库检索记录 / 调参日志（倒序，自动滚动）
- 资源占用：进程 CPU / 内存实时显示（对应「低资源占用」原则）
- 直连感知服务：感知端口不可用时页面显示红色告警而不是空白页

## v1.2 —— 自动汇报 & 多渠道通知（小版本）

> 目标：Agent 打完一局**主动汇报**，不再只能被 `report` 命令叫。
> 落地：`report_notifier.py` + 每局结束自动生成 markdown 报告。

- 每局结束(play 退出)自动写一份 `run_logs/report_YYYYMMDD_HHMMSS.md`，含：本轮回合 / 死亡 / 学到的新战术 / 调参变化
- 可配置推送：本地文件(默认) / Webhook(可选，`config.yaml` 配置 URL 即可)
- `notify` 命令手动触发报告一次
- 报告与 `agent_state.json` 打通，`report` 命令读同一份数据

## v1.3 —— 一键登记新游戏档案（小版本）

> 目标：把"通用化"落到实操——**想换游戏不再麻烦**，跑一个向导就登记好。
> 落地：`tools/add_game.py` 交互向导。

- 向导问答：游戏名 / 描述 / 感知端口 / 实体分类 / 威胁分 / 稀有度 / 套装关键词
- 生成可直接用的 `game_profiles/<name>.yaml`（和 `florr.yaml` 同构）
- 自动把 `config.yaml` 的 `agent.game` 切到新游戏
- 向导输入可读现有的 `florr.yaml` 作为默认参考，降低门槛

## v1.4 —— 断点续玩 & 会话记忆（小版本）

> 目标：重启 / 崩溃后**不从头来**，自动接上上次进度。
> 落地：`session.py` + 强化 `agent_state.json`。

- 记录：上次游戏 / 回合数 / 死亡数 / 已加载 Skill / brief / 上次调参结论
- `play` 启动时自动检测上次未完成的进度，提示"上次玩到 N 回合，是否续玩"
- `auto` 全链路到 play 前自动恢复 brief / Skill，避免每次重来
- 迁移：旧的 `agent_state.json` 无 `brief` 字段也能平滑兼容

---

# 远期大版本定位（v2.0 → v5.0）

## v2.0 —— Florr.io 高级拟人玩家

正式深度开发 Florr.io 专属高级功能，达到"像高水平人类玩家"的水平：
全局长期战略规划、多人博弈预判、自主战术创新、自适应难度、识破玩家套路（诱骗/假撤退/埋伏）、MCP 多智能体互联组队、高阶花瓣套装体系、全可视化监控、资源调度器（J1900 上 7×24 稳定）、知识库导入导出。

> 详细功能清单在进入 v2.0 开发时再展开。

## v3.0 —— 通用 2D 游戏智能体（转折点）

架构解耦：把智能体与 Florr.io 解耦，变成可复用的 2D 网页游戏框架。
- **通用核心层**：VLM 感知 / MCP 知识库 / 运动预判 / 动作规划 / 键鼠控制 / 资源调度 / 视频学习复盘
- **游戏适配层**：每个游戏一份轻量配置（实体定义 / 规则描述 / 状态评估 / 战术模板）
- 新游戏只需写适配器，不改核心；内置 Florr.io 适配器 + 一个示例 2D 游戏
- 适配器热加载、沙箱安全模式

> 限制：仅 2D 网页游戏，新游戏仍需人工写适配器。

## v4.0 —— 完美 2D 游戏智能体（自学习适配）

大多数 2D 游戏无需人工写码：看教程视频 + 观察画面自动生成适配器；反复游玩自动学会规则与胜负条件；自动实体分类；跨游戏战术迁移；自动防卡死恢复；知识库按游戏分类管理。

> 限制：仍限 2D，极端反自动化检测的游戏仍会失败。

## v5.0 —— 首次 3D 游戏支持

把通用核心扩展到 3D：3D 画面感知与深度估计、X/Y/Z 三维运动预判、3D 适配层、3D 游戏自学习；完全向后兼容 v4.0 的全部 2D 适配器；MCP 接口与知识格式不变。

> 限制：3D 早期阶段，复杂渲染 / 遮挡 / 强反作弊仍困难。

---

## 附：贯穿所有版本的原则

1. **小硬盘友好**：临时文件用完即删，日志滚动，知识库体积受控
2. **低配置可跑**：预判与战斗评估纯数学运算，LLM/VLM 全走云端 API
3. **拟人优先**：抖动、停顿、非完美操作，降低"脚本感"
4. **知识本地化**：所有学到的东西都沉淀为本地 MD，可导出可迁移
5. **不死优先**：打不过就跑，保命持续作战
