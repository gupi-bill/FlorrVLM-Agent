# 安全边界（被外部 Agent 调用时）

`Universal-Game-Framework` 是一个以 **MCP 能力包** 形态分发给其他 Agent 的项目：外部 Agent 安装后，通过 MCP 协议调用本仓库提供的工具（感知、动作、知识库读写等）。本文件规定它被外部调用时的**强制安全边界**。

> 适用版本：v2.1.0-mcp 及以后。MCP 入口见 `mcp_server.py`，CLI 入口见 `ugf_cli.py`。

## 1. 默认 Dry-Run（不执行真实副作用）

- 所有"会改变外部世界"的工具（键鼠动作、文件落盘、网络请求）**默认走 dry-run**。
- 触发方式：环境变量 `UGF_DRY_RUN=1`（缺省即视为开启）。
- dry-run 行为：只校验参数、返回"本应发生的动作"描述，**不执行任何真实键鼠/进程副作用**。
- 所有"本该发生的动作"写入 `run_logs/dryrun_actions.log`，便于离线验收与回放核对。
- 绝不因 dry-run 关闭而绕过参数校验——**校验先于执行分支**（`mcp_server.py` 中合法动作集合先定义、先校验）。

## 2. 路径沙箱（禁止越界读写）

- 所有文件操作锚定在仓库根：`BASE_DIR = 项目根目录`。
- 知识库读写限制在 `KB_DIR = BASE_DIR/knowledge_md` 内：
  - 游戏名、文件名经 `safe_game` / `safe_file` 清洗，`os.path.normpath` 归一化；
  - 任何试图跳出 `KB_DIR` 的路径（如 `../`、`绝对路径`）一律拒绝。
- 仅 `knowledge_md/` 与 `run_logs/` 为预期可写区；备份落在 `knowledge_archive/`。
- 不允许通过工具参数传入任意路径执行读写。

## 3. 无任意代码/命令执行

- 工具**不提供** `eval` / `exec` / shell 执行类接口。
- 动作集合为**白名单枚举**（"合法动作集"），未在其中定义的动作直接校验失败，不会被记成成功。
- 不在工具内部拉起未声明的子进程去改变宿主环境。

## 4. 网络出口

- 默认不发起不可控的出站请求。
- dry-run 下连"感知取帧"也降级为**进程内 mock**（`_dryrun_log` 记录），不依赖外部常驻服务。
- 真实模式下的任何网络调用必须是配置驱动、可审计的，禁止把用户内容外泄到非声明端点。

## 5. HTTP 传输鉴权（若对外暴露）

- 本包支持 stdio 与 HTTP 两种 MCP 传输。**stdio 传输绝不可直接暴露到网络。**
- 若经 HTTP 暴露（如 `ugf-mcp` 起 HTTP 端点），**必须**在反向代理或入口层强制 `Authorization: Bearer <token>` 校验，未带合法 token 直接 401。
- token 由调用方自管，**本仓库不内置任何凭证**，也不读取宿主的无关凭据。
- 建议：仅在内网 / 已鉴权的隧道中使用 HTTP 模式；公网暴露需额外加 mTLS 或网关。

## 6. 数据处理与留存

- `run_logs/` 可能含"动作描述"文本，属本地运行日志，不应随包分发或上传。
- `knowledge_md/`、`knowledge_archive/` 为用户/agent 自有知识数据，包本身不携带敏感信息。
- 安装脚本（`ugf-install`）只写入声明的入口与配置，不读取或上传宿主其他目录。

## 7. 漏洞反馈

- 安全问题请通过仓库 Issue 私信维护者（`gupi-bill`），不要在公开 Issue 贴可利用细节。
- 已知限制：本包不自带运行时沙箱隔离，调用方应把它当作"可信但最小权限"的能力包，而非"完全隔离的不可信代码"。

---
*本文件对应计划 `devplan/PLAN_NIGHT_0924.md` 的 S3。机制依据 `mcp_server.py` 实测（dry-run / 路径沙箱 / 白名单动作 / 进程内 mock 降级）。*
